version: 1.0
runtime: python311
build:
  commands:
    pre-build:
      - python -m pip install --upgrade pip
    build:
      - python -m pip install --no-cache-dir -r requirements.txt
run:
  command: python -m gunicorn --bind 0.0.0.0:8080 --workers 2 --threads 2 app:app
  network:
    port: 8080